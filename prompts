when clicked on im a brands
iill all that info then take them to a new page that shows them creators to collab with below that
the matchmaking options where they can swipe the creators
 and then match with the ones which they meet requirement of 
make a header for that cbrand page creaotrs option that leads that to the creators that are available for collabe 
then work where they can post about their businnes and brandand 
then a collab that lead them to this collab page where they get notifications that they have accepted a creators collab req
or if the creator has messaged them  like in a square box like notifcations manner 
then a chat options 
that leads them to a chat where the  chat open in the right ( a little big) and the the ppl they have talked to or brands show on the left 
and the part where its writtin its contagious for a reason chnage it to its contagious for a good reason and the font that is used there instead of that use <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Elsie:wght@400;900&display=swap" rel="stylesheet"> this everywhere accordingly accept the infleunza word
